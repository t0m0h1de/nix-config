# ===============================================
# My Custom Zsh Settings (Pure Shell Config)
# ===============================================

# nixの設定
if [ -e ${HOME}/.nix-profile/etc/profile.d/nix.sh ]; then
  . ${HOME}/.nix-profile/etc/profile.d/nix.sh;
fi

# --- 補完設定 (スタイルのみ) ---
# compinit は Home Manager が自動実行済み
zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}' 'r:|[._-]=* r:|=*' 'l:|=* r:|=*'

# --- ヒストリ設定 ---
setopt hist_ignore_dups
setopt share_history

# --- エイリアス ---
alias ls='ls --color=auto'
alias ll='ls -alF'
alias la='ls -A'
alias j!=jbang

# --- gcloud (公式インストーラー管理) ---
if [ -d "$HOME/.gcloud/google-cloud-sdk/bin" ]; then
  export PATH="$HOME/.gcloud/google-cloud-sdk/bin:$PATH"
fi

# --- AI CLI aliases (npx経由) ---
alias codex='npx @openai/codex'
alias gemini-cli='npx @google/gemini-cli'
alias copilot='npx @github/copilot'
alias jules='npx @google/jules'
alias claude='npx @anthropic-ai/claude-code'

# --- キーバインド (Plugin依存) ---
# 注意: zaw が有効な環境でのみ Ctrl+h を zaw-history に割り当てる
if zle -l | grep -qx 'zaw-history'; then
  bindkey '^H' zaw-history
fi

# --- Plugin設定 ---
# zsh-vi-mode の設定 (ZVM_VI_INSERT_ESCAPE_BINDKEY, zvm_after_init) は
# プラグイン読み込みより前に定義する必要があるため zsh.nix 側で設定

# ghq + fzfのユーティリティ(リポジトリ一覧から選択して移動するための関数)
function ghq-fzf() {
  local src=$(ghq list | fzf --preview "ls -laTp $(ghq root)/{} | tail -n+2 | head -n 20")
  if [ -n "$src" ]; then
    BUFFER="cd $(ghq root)/$src"
    zle accept-line
  fi
  zle -R -c
}
zle -N ghq-fzf
bindkey '^g' ghq-fzf  # Ctrl+g で発動

# 通常ディレクトリ移動用コマンド:
# - 引数あり: 指定パス配下を fzf で選んで cd
# - 引数なし: カレント配下を fzf で選んで cd
function cd-nav() {
  if [ "$#" -gt 1 ]; then
    echo "usage: cd-nav [root-path]" >&2
    return 1
  fi

  local root=${1:-.}
  if [ ! -d "$root" ]; then
    echo "cd-nav: no such directory: $root" >&2
    return 1
  fi

  local root_abs
  root_abs=$(cd "$root" 2>/dev/null && pwd -P) || return 1

  local dir
  dir=$(
    cd "$root_abs" 2>/dev/null || exit 1
    fd --type d --hidden --follow --exclude .git --exclude node_modules . . 2>/dev/null \
      | fzf --prompt="dir> " --preview='ls -la {} | head -n 40'
  )

  if [ -n "$dir" ]; then
    cd "$root_abs/$dir"
  fi
}
# Force redefine quietly so repeated sourcing (e.g. nested shells in tmux)
# stays idempotent without duplicate definitions.
abbr --force --quiet cdn='cd-nav'
abbr --force --quiet kube='kubectl'
abbr --force --quiet kub='kubectl'
